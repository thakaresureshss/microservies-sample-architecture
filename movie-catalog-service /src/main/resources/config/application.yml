server:
   port: 8080
   error:
      include-stacktrace: on_trace_param
spring:
   application:
      name: movie-catalog-service
eureka:
   client:
      service-url:
         default-zone: http://localhost:8761/eureka